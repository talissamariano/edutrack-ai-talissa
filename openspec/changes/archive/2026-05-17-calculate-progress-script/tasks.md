## 1. Script calculate_progress.py

- [x] 1.1 Criar o diretório `scripts/` e o arquivo `scripts/calculate_progress.py`
- [x] 1.2 Implementar a função `calculate_progress(tasks)` que conta `total` e `completed` (status == "done")
- [x] 1.3 Calcular `percentage` como `(completed / total) * 100`, tratando `total == 0` (retornar 0.0)
- [x] 1.4 Envolver o cálculo em `try/except` e retornar o resultado como string JSON (`json.dumps`)
- [x] 1.5 Adicionar bloco `if __name__ == "__main__":` com um exemplo de uso
